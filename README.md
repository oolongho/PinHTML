# PinHTML App

PinHTML 原型标注工具

Vite + React 19 + TypeScript strict + Tailwind 4 + shadcn/ui。

## 开发命令

```bash
npm run dev        # 本地开发（HMR）
npm run build      # 类型检查 + 构建，产出单文件 dist/PinHTML.html
npm run typecheck  # 仅类型检查
```
